[project]
name = "otel-mapper"
version = "0.1.0"
description = "OTLP/HTTP JSON span to AgentRun/Edge candidate mapping (Apache-2.0)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
