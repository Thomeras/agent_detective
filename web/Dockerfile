# Web UI placeholder. Build context is the repo root.
# TODO(M7): replace with the real React + Vite + TypeScript build
# (multi-stage build, then serve the bundle with nginx).
FROM nginx:1.27-alpine

COPY web/index.html /usr/share/nginx/html/index.html
